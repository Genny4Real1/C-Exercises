#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main (bool weekday, bool vacation) {
    bool = weekday = false, vacation = false;

    if (weekday == false) {
	    i
    return 0;
}
